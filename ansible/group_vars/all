---

env: prod

httpd_port: 80
httpd_ssl_port: 443

ntp_servers:
  - "0.fr.pool.ntp.org"
  - "1.fr.pool.ntp.org"
  - "2.fr.pool.ntp.org"
  - "3.fr.pool.ntp.org"

ntp_fallback: ntp.ubuntu.com

timezone: Europe/Paris

web_directory: /var/www/

# Config nginx
nginx: {
    security: {
        server_tokens: off
    },
    gzip: {
        gzip_vary: on,
        gzip_proxied: any,
        gzip_comp_level: 6,
        gzip_buffers: "16 8k",
        gzip_http_version: "1.1",
        gzip_types: "text/plain text/css application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript"
    }
}

ssl_directory: {
    cert: "/etc/ssl/certs/",
    key: "/etc/ssl/private/"
}
